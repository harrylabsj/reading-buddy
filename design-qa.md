# 读书搭子 · 验收记录

final result: passed

## 比较对象和状态

- 视觉来源：`../设计方向-1.png`（1487 × 1058），以其首页层级、暖纸色、宋体标题、侧边导航为基准；书架与笔记页采用已批准方案中的对应流程。
- 桌面实现：`../reading-buddy-preview.png`（1440 × 1024）；CSS viewport 1440 × 1024，devicePixelRatio 1。
- 比较方式：同一次图像查看输入中同时打开源图与桌面实现，按接近一致的宽高比比较整体布局；源码示例日期为 9 月 12 日，实现已按当前日期推进到 9 月 13 日，时长与剩余目标随示例变化。没有将这些动态差异当作视觉错误。
- 移动实现：`../../work/qa/mobile-final.png`（390 × 844），另有修正前 `mobile-after.png`。
- 笔记页：`../../work/qa/desktop-notes.png`。浏览器截图存在缩放软化；DOM 样式检查确认无 CSS blur、transform 或页面缩放。小字号同时通过浏览器可访问树和实际交互确认，未宣称逐像素一致。

## 发现、修正和复验

1. P2：移动首页书籍封面与阅读提示重叠。改为明确的“封面/书名”第一行和完整阅读操作第二行，移除旧的负边距。390 × 844 的最后截图确认标题、进度和按钮完整，未被底部导航遮住。
2. P2：第一次网格调整后，旧规则仍让移动标题向左移出画面。已重置标题 margin 与 padding，再次截图确认已修复。
3. P2：继续阅读筛选将大量未开始的书列入首屏。改为有明确阅读进度或已设为主读的书；实际书架复验，默认显示 13 本，而全部电子书仍可检索。
4. 日期状态：测试跨过午夜，示例快照应随日期更新，同时保留保存的卡片/回顾。已修正并在页面确认 9 月 13 日与对应日统计一致。

没有剩余会阻碍核心操作的 P0/P1/P2 视觉问题。

## 五项视觉检查

- 字体：标题采用 Noto Serif SC / 系统宋体，正文采用系统中文无衬线；长真实书名限制行数并保留完整 title 与可访问名称。
- 布局：宽屏侧边导航、首页主读区与下方两列；窄屏下方五个入口，主内容预留导航高度。桌面检查 documentElement.scrollWidth 未超过 viewport。
- 颜色：暖纸白、琥珀按钮、轻分隔线、带文字的同步状态；避免只用色点解释失败。
- 图片：首页氛围图和示例封面为独立 ImageGen 资产；真实书籍使用接口封面。缺图用标准图标和文字说明，没有用示例书封面替代真实封面。
- 内容：明确示例/真实数据、本机整理/WorkBuddy 草稿、同步时间和缺失口径。原个人操作系统导航及其它领域信息未进入应用。

原图中的书籍焦点、装饰摄影与部分字号不是逐像素复制，属于独立素材和响应式实现的可接受差异。当前演示仅有一本虚构书，完整书架由真实数据承接。

## 实际验证

- 独立接口读取真实微信读书：8601 本电子书、首批 41 条笔记；本周/月统计、真实封面、主读进度可见。
- 浏览器：今日阅读、书架、笔记回顾、复盘导航；书名搜索找到正确书籍；示例回顾输入、生成草稿、保存、刷新后恢复；设置入口；桌面和 390px 窄屏。
- 控制台 error/warn 查询为空。
- `npm test`：9 项通过，含独立 HTTP 进程、CSRF/Origin、设置、卡片、备份幂等合并、重启恢复、Markdown 下载响应及内容一致性。
- `npm run test:mcp`：1 项通过，含握手、6 个工具、MCP Apps 资源、指定书籍笔记和确认后写回。
- `npm run build`：通过，前端及 MCP Apps HTML 已生成。

浏览器工具没有捕获到第一次 Blob 下载事件，不能声称该次文件已落盘；已增加并通过 HTTP 测试验证保存卡片的直接 Markdown 附件下载接口。导出内容和备份恢复已经验证。

## 未验证事项

- WorkBuddy 客户端本地配置导入、实际 MCP Apps 宿主握手/渲染与打开本地链接。
- 平台应用注册、审核、通用安装分发、OAuth 自动绑定与公开发布。
- 手机真机、跨设备自动同步和全部笔记内容的全量导出。

上述平台工作不影响已经运行的独立个人桌面版本，不应被表述为已完成上架。
