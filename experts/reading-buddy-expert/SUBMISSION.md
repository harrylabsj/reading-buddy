# 读书搭子专家 · 提交前清单

当前候选物由项目根目录的 `npm run package:expert` 生成：

- `dist/expert/reading-buddy-expert-0.3.0.zip`
- `dist/expert/reading-buddy-expert-0.3.0.zip.sha256`

提交前在 WorkBuddy 桌面端完成以下验证；不要使用真实书架、笔记或 API Key 录制公开案例。

1. 导入 ZIP，确认市场卡片职称为“读书搭子”、花名为“海纳·读书专家”，教育与学习分类、3 个快捷提问和 512×512 头像正确。
2. 召唤专家，确认内置 `reading-buddy` Skill 已加载，且没有 OAuth、MCP 或连接器依赖引导。
3. 在一台已安装 Node.js 20+ 的测试电脑上，使用虚构 Key 文件验证 `auth import`、`auth status`、`auth revoke` 的提示；不要将 Key 粘贴进对话。
4. 用 `--mode demo` 验证“今天读什么”“整理笔记”“本周复盘”和本地 HTML 报告。报告应只显示虚构数据，打开时不应请求外部资源。
5. 仅在用户主动授权后，使用测试账号验证一次真实同步；检查卡片保存、备份、报告均保存在本机，且专家不读取整库笔记进入模型任务。
6. 确认专家详情页与提示词没有承诺自动云端同步、独立 Buddy 应用、OAuth 登录或公开分享用户阅读数据。

作者公开联系邮箱已由用户明确指定为 `1711496337@qq.com`，后续发布沿用，不再重复询问；仅在用户主动要求时变更。

提交审核、公开发布、上传灵感案例属于外部变更，需单独执行；本清单不构成发布完成声明。
