# 读书搭子专家

这是 WorkBuddy 专家包的源码入口。发布形态为“专家 + 内置本地 Skill + 用户自己的阅读工作台”：专家负责对话引导和阅读方法，Skill 负责用户授权后的本机同步、整理、保存与报告生成。

不包含 MCP 服务、连接器依赖、Buddy 应用 OAuth、用户书架、笔记、备份或微信读书 API Key。

请用项目根目录的 `npm run package:expert` 生成可上传的 ZIP。生成包仍需先在 WorkBuddy 客户端导入和验收，再提交专家审核；不要将生成的示例报告或任何个人阅读数据上传到公开市场或灵感案例。
