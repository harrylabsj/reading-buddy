# 读书搭子 · WorkBuddy 发布记录

本记录保存平台提交结果，不包含登录凭证或微信读书数据。

## 专家已提交审核（2026-09-14）

用户明确授权发布后，已通过正常微信快捷登录进入已认证企业主体，上传更新后的专家包，核对信息并提交审核。平台回执为“已提交审核，预计 7 个工作日内出结果”，资产列表再次确认状态为“审核中”；不代表审核通过或已公开上架。

| 项目 | 已提交信息 |
| --- | --- |
| 类型 | 专家 |
| 专家 ID | oe_cab59592a0d1949b |
| 职称 | 读书搭子 |
| 花名 | 海纳·读书专家 |
| 作者公开联系邮箱 | 1711496337@qq.com |
| 版本 | 0.3.0 |
| 市场分类 | 教育学习 |
| 服务类目 | 工具 - 备忘录 |
| 平台状态 | 审核中 |
| 提交包 | reading-buddy-expert-0.3.0.zip |
| SHA-256 | c6d542d61c5aac38f1782f56e30e16acad2f833693d5dec21971aacf0ae28edf |

作者联系邮箱、职称和花名已写入 AGENTS.md，后续沿用，不再重复询问。新信息同时保存在专家 plugin.json 与 Agent 定义中。提交包包含本地 Skill 和脚本，不含用户阅读数据或微信读书凭证。

证据：本任务交付目录 `outputs/workbuddy-expert-review.png`。审核进度可在[专家管理](https://open.workbuddy.cn/expert/all)查看。此前连接器审核记录继续保留；本轮未修改其他专家或连接器。

本轮完成的是平台上传、信息确认及审核提交；未新增客户端端到端导入或真实微信读书同步验证记录。

> 2026-09-14 产品形态已调整为“读书搭子专家 + 内置本地 Skill + 用户自己的阅读工作台”。下方连接器审核记录与 Buddy 应用调研事实继续保留；Buddy 应用 OAuth 不再是当前发布路径的阻塞项。专家候选包与发布前验证见 `experts/reading-buddy-expert/` 和项目根目录 README。

## 已完成

- 按用户明确授权，接受 WorkBuddy 开放平台开发者服务协议、隐私保护指引和数据处理协议。
- 使用现有微信快捷登录，进入已认证主体“北京海纳福星文化传媒有限公司”。
- 检查已有资产，无重复的“读书搭子”。
- 上传并由平台解析 `reading-buddy-workbuddy-local-v0.2.0.zip`。
- 提交连接器审核，并在资产列表核实状态为“审核中”。

| 项目 | 平台记录 |
| --- | --- |
| 资产类型 | 连接器 |
| 名称 | 读书搭子 |
| 版本 | 0.2.0 |
| 连接器 ID | oc_687a66bbf2d16f41 |
| 分类 | 工具 - 备忘录 |
| 状态 | 审核中 |
| 平台预估 | 7 个工作日内出结果，不是审核通过保证 |

[连接器管理](https://open.workbuddy.cn/connector/all)

## Buddy 应用入口尚未提交

实际创建表单强制要求：头像（至少96×96、最多100KB的JPG/PNG）、名称、简介、服务类目、授权列表及 OAuth 回调地址。授权列表注明首次创建后无法更改。

已核实权限选项包括读取个人资料、创建云端任务、读取云端任务、读取本地助理历史及执行任务等。没有擅自选择这些额外权限。

目前实现使用本地 stdio + 微信读书 API Key，没有实现 Buddy 应用 Open API 的 OAuth 回调。未填写虚假的回调地址、未新增云端服务、未提交不完整的应用创建请求。名称和简介只在表单中填写，不能视为已保存的应用草稿。

官方文档后续配置支持“跳过首次绑定应用”，但本次实际创建表单仍将 OAuth 回调地址标为必填。不能据此声称本地 Buddy 应用必然需要云端，也不能假定可以省略创建所需配置。

继续需要明确：该平台是否提供纯本地应用的免回调/免额外权限创建路径，或设计并验证实际可用的本地回调方案。此项尚未完成。

[Buddy 应用管理](https://open.workbuddy.cn/buddy-app/all) · [官方文档](https://open.workbuddy.cn/docs/buddy-app)

## 证据

- `workbuddy-connector-review.png`：读书搭子 v0.2.0 在资产列表显示审核中。
- `workbuddy-buddy-app-requirements.png`：Buddy 应用创建页的强制配置要求。

已提交审核不代表已上架；尚未创建审核状态监控，也未对外联系平台人员。

## 继续推进时的新核实结果

- 已生成并上传符合规格的应用头像：256×256 PNG，3596 字节；本地材料为 `reading-buddy-app-icon.png`。
- 应用名称、简介与「工具 - 备忘录」类目已填写。授权列表为空、回调地址为空时，提交按钮仍处于禁用状态。
- 只读核对创建页已加载的公开前端代码，确认基础校验要求 `scopes.length > 0`；回调地址需有 1—3 项且全部有效。
- 官方创建页 URL 校验允许 HTTPS，也允许主机为 `localhost` 或 `127.0.0.1`、带非根路径且无 fragment 的 HTTP URL。
- 创建后的 `home.authConfig.skipJump` 配置不参与基础创建校验。因此「后续跳过首次绑定」不能直接省略当前创建所需的注册字段。
- 此结果只能证明前端允许本地回环地址，不能证明本地 OAuth 换 Token、应用包安装目录及 WorkBuddy 宿主渲染已验证。
- 再次查看连接器列表，读书搭子 `oc_687a66bbf2d16f41` 仍为审核中。

证据：`workbuddy-creation-ready-except-oauth.png`。

## 官方支持咨询已发送

用户明确授权后，已使用已授权的 Gmail 连接器，从 `jianghd2042@gmail.com` 向 `openworkbuddy@tencent.com` 发送接入咨询。Gmail 返回 `SENT` 标签，消息 ID 与会话 ID 均为 `1a098755531dffe6`。邮件无附件，仅包含产品接入问题及连接器 ID，不含个人书架、笔记或凭证。

咨询范围：纯本地 Token 型 Buddy 应用的最小权限、跳过绑定后的回调要求、本地公共客户端授权方式，以及 stdio 程序和 HTML 资源的安装目录约定。当前等待官方答复；尚未配置自动检查或提醒。
