# 可通过设置页面连接；或在启动进程的环境中设置凭证。不要将真实凭证提交到版本库。
WEREAD_API_KEY=
PORT=3788
# READING_DATA_DIR=/your/private/reading-buddy-data
# Skill 模式的凭证目录（默认用户级配置目录，测试时可覆盖）：
# READING_CONFIG_DIR=/your/private/reading-buddy-config
# 免连接器 Skill 命令行入口：node server/cli.mjs --help
